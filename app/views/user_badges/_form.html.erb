<%= form_for(@user_badge) do |f| %>
  <% if @user_badge.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_badge.errors.count, "error") %> prohibited this user_badge from being saved:</h2>

      <ul>
      <% @user_badge.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :badge_image %><br>
    <%= f.text_field :badge_image %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
